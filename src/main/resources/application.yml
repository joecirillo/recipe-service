spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Foodies_Finds_DB
    username: postgres
    password: LuckyHusky
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true