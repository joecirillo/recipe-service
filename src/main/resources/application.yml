spring:
  config:
    import: "optional:aws-parameterstore:/foodies-finds/recipe-service/"
  cloud:
    aws:
      region:
        static: us-east-2
  jpa:
    hibernate:
      ddl-auto: validate
      format_sql: true
    show-sql: true
